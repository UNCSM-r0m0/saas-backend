export const AUTH_PATTERNS = {
  health: 'auth.health',
  register: 'auth.register',
  validateUser: 'auth.validateUser',
  validateOAuthUser: 'auth.validateOAuthUser',
  issueTokens: 'auth.issueTokens',
  refresh: 'auth.refresh',
  revoke: 'auth.revoke',
} as const;
